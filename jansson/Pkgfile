description=" The Jansson package contains a library used to encode, decode, and manipulate JSON data. "
name=jansson
version=2.15.0
release=1
packager=alexis
source=("https://github.com/akheron/jansson/releases/download/v2.15.0/jansson-2.15.0.tar.bz2")

build() {
cd $name-$version
./configure --prefix=/usr --disable-static &&
make
make DESTDIR=$PKG install
#]
}
