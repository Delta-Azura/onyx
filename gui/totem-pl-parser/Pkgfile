description=" The Totem PL Parser package contains a simple GObject-based library used to parse multiple playlist formats. "
name=totem-pl-parser
version=3.26.7
release=1
packager=alexis
source=("https://download.gnome.org/sources/totem-pl-parser/3.26/totem-pl-parser-${version}.tar.xz")

build() {
cd $name-$version
mkdir build &&
cd    build &&

meson setup --prefix=/usr --buildtype=release .. &&
ninja
DESTDIR=$PKG ninja install
}
