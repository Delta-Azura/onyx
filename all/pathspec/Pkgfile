description=" Pathspec is a utility library for pattern matching of file paths. "
name=pathspec
version=1.1.1
release=1
packager=alexis
source=("https://files.pythonhosted.org/packages/source/p/pathspec/pathspec-${version}.tar.gz")

build() {
cd $name-$version

pip3 wheel -w dist \
    --no-build-isolation \
    --no-deps $PWD
  pip3 install --no-index --ignore-installed \
    --find-links dist \
    --no-cache-dir    \
    --no-user         \
    --root=$PKG       \
    pathspec
}
