description=" The GNOME Keyring package contains a daemon that keeps passwords and other secrets for users. "
name=gnome-keyring
version=48.0
release=1
packager=alexis
source=("https://download.gnome.org/sources/gnome-keyring/48/gnome-keyring-48.0.tar.xz")

build() {
cd $name-$version
sed -i 's:"/desktop:"/org:' schema/*.xml &&

mkdir build-gkr &&
cd    build-gkr &&

meson setup --prefix=/usr --buildtype=release -D manpage=false .. &&
ninja
DESTDIR=$PKG ninja install
}
